[project]
name = "projeto-automacao-ia"
version = "0.2.0"
description = "Semanas 1-2 da trilha pratica: classificador de feedbacks com IA (CLI + API FastAPI)"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "python-dotenv",
    "fastapi",
    "uvicorn[standard]",
    "psycopg[binary]>=3.3.6",
]
